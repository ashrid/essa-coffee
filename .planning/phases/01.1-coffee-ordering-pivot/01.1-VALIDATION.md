---
phase: 01.1
slug: coffee-ordering-pivot
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-22
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None (no test suite in project — grep + tsc verification) |
| **Config file** | none |
| **Quick run command** | `npx tsc --noEmit` |
| **Full suite command** | `npm run build` |
| **Estimated runtime** | ~30–60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit`
- **After every plan wave:** Run `npm run build`
- **Before `/gsd:verify-work`:** Full build must succeed + manual smoke test
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| schema-migration | 01 | 1 | D-01/D-02 | grep | `grep -r "stockQuantity\|careInstructions\|lowStockThreshold" prisma/schema.prisma` → 0 matches | ✅ | ⬜ pending |
| validators-update | 01 | 1 | D-01/D-02 | grep | `grep -r "stockQuantity\|careInstructions\|lowStockThreshold" lib/validators.ts` → 0 matches | ✅ | ⬜ pending |
| orders-refactor | 01 | 1 | D-05 | grep | `grep "decrement" lib/orders.ts` → 0 matches | ✅ | ⬜ pending |
| cart-store-update | 01 | 1 | D-06 | grep | `grep "stockQuantity" lib/cart-store.ts` → 0 matches | ✅ | ⬜ pending |
| admin-form-update | 01 | 2 | D-14/D-15 | grep | `grep "careInstructions\|stockQuantity\|lowStockThreshold" components/admin/ProductForm.tsx` → 0 matches | ✅ | ⬜ pending |
| dashboard-stats | 01 | 2 | D-16/D-17 | grep | `grep "lowStockCount\|Low Stock" components/admin/DashboardStats.tsx` → 0 matches | ✅ | ⬜ pending |
| branding-sweep | 02 | 2 | D-08/D-18 | grep | `grep -r "ShopSeeds" app/ components/ lib/ --include="*.tsx" --include="*.ts"` → 0 matches | ✅ | ⬜ pending |
| palette-update | 02 | 2 | D-10 | grep | `grep "forest-600" tailwind.config.ts` + value contains espresso tone not green | ✅ | ⬜ pending |
| seed-data | 03 | 3 | D-11/D-12 | grep | `grep "Hot Drinks\|Iced Drinks\|Specialty\|Food" prisma/seed.ts` → matches exist | ✅ | ⬜ pending |
| ts-compile | all | end | — | compile | `npx tsc --noEmit` exits 0 | ✅ | ⬜ pending |
| build-success | all | end | — | build | `npm run build` exits 0 | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No test framework installation needed — verification relies on TypeScript compiler and grep commands only.

*Existing infrastructure (tsc, Next.js build) covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin form shows isAvailable + isFeatured toggles | D-14/D-15 | UI render check | Log in to /admin, open Add Product — confirm isAvailable checkbox and isFeatured checkbox visible; Stock Quantity, Low Stock Threshold, Care Instructions fields absent |
| Store product page shows no care instructions | D-19 | UI render check | Open any product detail page — confirm "Care & Growing Instructions" section absent |
| Homepage hero shows Essa Cafe copy | D-08/D-20 | UI render check | Load homepage — confirm "Essa Cafe" in header, "Order ahead, pick up fresh" in subtitle area |
| AvailabilityBadge shows Available/Unavailable | D-07 | UI render check | View product with isAvailable=true → shows "Available" badge; toggle to false → shows "Unavailable" |
| Color palette looks espresso/caramel | D-10 | Visual check | Load any store page — nav/buttons should be dark espresso brown, not forest green |
| Checkout creates order without stock error | D-05 | Flow check | Add item to cart → checkout → confirm order created in /admin/orders |
| Dashboard shows 2 stats only | D-16 | UI render check | Load /admin — confirm only New Orders + Today's Revenue cards (no Low Stock Alerts) |

---

## Validation Sign-Off

- [ ] All tasks have automated grep/compile verify or manual instructions
- [ ] Sampling continuity: tsc runs after every task
- [ ] Wave 0: No stubs needed
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s (tsc completes in ~30s)
- [ ] `nyquist_compliant: true` set in frontmatter after all tasks verified

**Approval:** pending
