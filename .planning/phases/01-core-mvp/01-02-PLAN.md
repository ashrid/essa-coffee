---
phase: 01-core-mvp
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(store)/layout.tsx
  - app/(store)/page.tsx
  - app/(store)/shop/page.tsx
  - app/(store)/shop/[slug]/page.tsx
  - app/(store)/pickup-info/page.tsx
  - components/store/ProductCard.tsx
  - components/store/ProductGrid.tsx
  - components/store/CategorySidebar.tsx
  - components/store/SearchBar.tsx
  - components/store/FeaturedProducts.tsx
  - components/store/Header.tsx
  - components/store/Footer.tsx
  - components/store/ProductImageCarousel.tsx
  - components/store/RelatedProducts.tsx
  - components/store/EmptyState.tsx
  - components/store/StockBadge.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage shows featured products section at top, then full catalog grid below"
    - "Product catalog renders in 2-3 column grid (desktop) and 2 columns (mobile)"
    - "Left sidebar shows category filters, in-stock toggle, and price sort"
    - "Out-of-stock products display grayed out with out-of-stock badge"
    - "Product detail page shows image carousel, description, care instructions, and related products"
    - "Pickup info page shows address, hours, and directions"
    - "Client-side search filters products by name/description without page reload"
    - "Empty category state shows friendly message (not blank)"
  artifacts:
    - path: "app/(store)/layout.tsx"
      provides: "Store layout with header and footer"
      contains: "Header"
    - path: "app/(store)/page.tsx"
      provides: "Homepage with featured + catalog sections"
      contains: "FeaturedProducts"
    - path: "app/(store)/shop/[slug]/page.tsx"
      provides: "Product detail page with ISR"
      contains: "generateStaticParams"
    - path: "components/store/ProductCard.tsx"
      provides: "Product card with image, name, price, stock badge, add-to-cart"
      exports: ["ProductCard"]
    - path: "components/store/CategorySidebar.tsx"
      provides: "Category filter sidebar with in-stock toggle and price sort"
      exports: ["CategorySidebar"]
    - path: "app/(store)/pickup-info/page.tsx"
      provides: "Pickup location, hours, directions"
      contains: "pickup"
  key_links:
    - from: "app/(store)/shop/page.tsx"
      to: "components/store/CategorySidebar.tsx"
      via: "URL search params for filter state"
      pattern: "useSearchParams|searchParams"
    - from: "app/(store)/page.tsx"
      to: "prisma.product.findMany"
      via: "Server Component fetch"
      pattern: "prisma\\.product\\.findMany"
    - from: "app/(store)/shop/[slug]/page.tsx"
      to: "prisma.product.findUnique"
      via: "Server Component with generateStaticParams"
      pattern: "generateStaticParams"
---

<objective>
Build the public storefront UI: homepage, product catalog with filtering and search, product detail page, and pickup info page. All catalog data fetched server-side via Prisma.

Purpose: This is the primary customer-facing experience. Products must be browsable, filterable, and searchable before checkout can work.
Output: Full storefront with navigation, product grid, sidebar filters, product detail, and pickup info page.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-mvp/01-CONTEXT.md
@.planning/phases/01-core-mvp/01-RESEARCH.md
@.planning/phases/01-core-mvp/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store layout, header, footer, and shared components</name>
  <files>
    app/(store)/layout.tsx
    components/store/Header.tsx
    components/store/Footer.tsx
    components/store/StockBadge.tsx
    components/store/EmptyState.tsx
    app/(store)/pickup-info/page.tsx
  </files>
  <action>
    Create `app/(store)/layout.tsx` — wraps all store pages with Header + Footer. Header contains:
    - Logo/store name (placeholder "ShopSeeds" text in forest-600, bold, large) using Next.js Link to `/`
    - Nav links: Shop (`/shop`), Pickup Info (`/pickup-info`)
    - Cart icon button (lucide-react `ShoppingCart`) with item count badge — reads from Zustand cart store (client component island via `use client`)
    - Mobile hamburger menu that shows nav links in a sheet/drawer
    - Background: cream-50 or white, border-b in cream-200
    - Sticky header on scroll

    Footer contains: store name, "Pickup only — no shipping", placeholder address, hours ("Mon–Sat 9am–6pm"), copyright year. Background forest-600, text cream-50.

    `components/store/StockBadge.tsx` — renders badge for stock status:
    - stockQuantity > lowStockThreshold: "In Stock" (sage-500 background)
    - 0 < stockQuantity <= lowStockThreshold: "Low Stock" (amber-500 background)
    - stockQuantity === 0: "Out of Stock" (red-500 background)
    Accepts `stockQuantity: number` and `lowStockThreshold: number` props.

    `components/store/EmptyState.tsx` — friendly empty state with a plant SVG illustration (simple inline SVG leaf/sprout, not external image), title "Nothing here yet", subtitle "Check back soon for new additions". Accepts optional `title` and `subtitle` props for customization.

    `app/(store)/pickup-info/page.tsx` — Server Component. Display:
    - Page title "Pickup Information"
    - Address section: placeholder "123 Green Street, Your City, State 00000"
    - Hours section: Mon–Fri 9am–6pm, Sat 9am–5pm, Sun Closed (in a table/list)
    - Instructions: "Order online, pay online or pay when you pick up. We'll email you when your order is ready."
    - Map placeholder: `<iframe>` linking to Google Maps with the placeholder address
    - Design: clean, earthy card layout with forest/cream colors
  </action>
  <verify>
    `npm run dev` → visit `http://localhost:3000/pickup-info` — page loads with address, hours, instructions.
    Visit `http://localhost:3000` — header shows "ShopSeeds" and nav links.
    Cart icon visible in header.
  </verify>
  <done>Store layout renders with sticky header (logo, nav links, cart icon) and footer (address, hours). Pickup info page shows address, hours, instructions. StockBadge and EmptyState components exist and export correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage + Product catalog with sidebar filters and search</name>
  <files>
    app/(store)/page.tsx
    app/(store)/shop/page.tsx
    components/store/ProductCard.tsx
    components/store/ProductGrid.tsx
    components/store/FeaturedProducts.tsx
    components/store/CategorySidebar.tsx
    components/store/SearchBar.tsx
  </files>
  <action>
    `components/store/ProductCard.tsx` — Renders single product as card:
    - Image: Next.js `<Image>` component, aspect-ratio square, object-cover. Show placeholder if no images. Gray out entire card (opacity-50 + grayscale CSS filter) if stockQuantity === 0
    - Product name (bold, forest-900, truncate 2 lines)
    - Category name (sage-500, small text)
    - Price (formatPrice from lib/utils, forest-600, font-semibold)
    - StockBadge component
    - "Add to Cart" button (forest-600 background, cream-50 text, hover: forest-700) — disabled + grayed if out of stock
    - `onClick` for add-to-cart calls Zustand cart store `addItem` (imported from `lib/cart-store` — stub for now, implemented in Plan 03). If store not yet available, use a placeholder noop.
    Props: `product: { id, name, slug, price, images, stockQuantity, lowStockThreshold, category: { name } }`
    Mark as `'use client'` for onClick.

    `components/store/ProductGrid.tsx` — Renders array of ProductCard in CSS grid. Props: `products[]`, optional `emptyMessage`. Uses EmptyState when array is empty. Grid: `grid-cols-2 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6`.

    `components/store/FeaturedProducts.tsx` — Section header "Featured Plants" + horizontal scroll row OR standard grid (2-3 items). Accepts `products[]`.

    `components/store/SearchBar.tsx` — `'use client'` component. `<input>` with search icon. On change, calls `onSearch(value)` prop with debounce (200ms using setTimeout). Controlled via `value` and `onSearch` props.

    `components/store/CategorySidebar.tsx` — `'use client'` component. Shows:
    - "Categories" heading + "All" option + list of category names (from `categories` prop)
    - Active category highlighted (forest-600 bg, cream text)
    - "In stock only" toggle (checkbox)
    - "Sort by price" select: "Default", "Low → High", "High → Low"
    - On any change, updates URL search params via `useRouter` and `useSearchParams` (client-side navigation without full reload)
    - Props: `categories: {id, name, slug}[]`, reads active state from URL search params

    `app/(store)/page.tsx` — Server Component (no 'use client'):
    - Fetch featured products: `prisma.product.findMany({ where: { isFeatured: true, stockQuantity: { gt: 0 } }, take: 4, include: { category: true }, orderBy: { createdAt: 'desc' } })`
    - Fetch all products: `prisma.product.findMany({ include: { category: true }, orderBy: { createdAt: 'desc' } })`
    - Render: FeaturedProducts section at top, then full ProductGrid below with heading "Our Collection"

    `app/(store)/shop/page.tsx` — Server Component. Reads URL `searchParams` for `category`, `inStock`, `sort`:
    - Fetch categories: `prisma.category.findMany({ orderBy: { name: 'asc' } })`
    - Fetch products with filters: where clause uses `categoryId` if `category` param set, `stockQuantity: { gt: 0 }` if `inStock` param set. Order by price asc/desc based on `sort` param, else newest first.
    - Render: left sidebar (CategorySidebar), SearchBar at top of grid area, ProductGrid. Client-side search filters the server-fetched products array in a wrapper `'use client'` component `ShopPageClient`.
    - Layout: `grid grid-cols-1 lg:grid-cols-[250px,1fr] gap-8`
    - Page title "Shop All Plants & Seeds" with product count shown.
  </action>
  <verify>
    `npm run dev` → visit `http://localhost:3000` — featured products and full catalog render with data from seed.
    Visit `http://localhost:3000/shop` — sidebar shows 3 categories, products displayed in grid.
    Click a category in sidebar — URL updates, products filter to that category.
    Toggle "In stock only" — out-of-stock products disappear.
    Type in search bar — products filter in real time.
  </verify>
  <done>Homepage shows featured products + full catalog. Shop page shows filterable/searchable grid with category sidebar. ProductCard renders with image, price, stock badge, and add-to-cart button. Empty states show friendly message.</done>
</task>

<task type="auto">
  <name>Task 3: Product detail page with image carousel, care instructions, related products</name>
  <files>
    app/(store)/shop/[slug]/page.tsx
    components/store/ProductImageCarousel.tsx
    components/store/RelatedProducts.tsx
  </files>
  <action>
    `components/store/ProductImageCarousel.tsx` — `'use client'` component. Swipeable image carousel:
    - Main large image display with left/right arrow buttons (lucide-react ChevronLeft/ChevronRight)
    - Thumbnail strip below showing all images, clicking switches main image
    - If only 1 image, hide arrows and thumbnail strip
    - Touch swipe support: track `touchstart` and `touchend` events, swipe left = next, swipe right = prev
    - Placeholder shown if `images` array is empty
    Props: `images: string[]`, `productName: string`

    `components/store/RelatedProducts.tsx` — Shows 2-3 products from same category, excluding current product:
    - Props: `products: Product[]`, `title?: string`
    - Renders as horizontal scroll on mobile, 3-column grid on desktop
    - Uses ProductCard component

    `app/(store)/shop/[slug]/page.tsx` — Server Component with ISR:
    ```typescript
    export async function generateStaticParams() {
      const products = await prisma.product.findMany({ select: { slug: true } });
      return products.map(p => ({ slug: p.slug }));
    }
    export const revalidate = 60; // ISR: revalidate every 60 seconds
    ```
    - Fetch product: `prisma.product.findUnique({ where: { slug }, include: { category: { include: { products: { take: 3, where: { slug: { not: slug }, stockQuantity: { gt: 0 } } } } } } })`
    - Return `notFound()` if product not found
    - Layout:
      - Back link "← Back to Shop" to `/shop`
      - Left: ProductImageCarousel (2/3 width on desktop)
      - Right: product name (h1, bold, large), category badge, price (large, forest-600), StockBadge
      - "Add to Cart" button (large, full-width on mobile, disabled if out of stock) — `'use client'` island component for interactivity
      - Product description section (renders HTML from rich text editor via `dangerouslySetInnerHTML` — safe because admin-entered content)
      - Care Instructions section (if careInstructions exists): "Care & Growing Instructions" heading, content
      - Related Products section at bottom: "More from [category name]"
    - Generate OG meta tags: `export async function generateMetadata({ params })`
  </action>
  <verify>
    `npm run dev` → visit `http://localhost:3000/shop/[slug-from-seed]` — product detail renders with carousel, description, care section, related products.
    Click left/right arrows on carousel — image changes.
    Thumbnail click changes main image.
    "Add to Cart" button present (may not work yet until Plan 03).
    Related products shows 2-3 products from same category.
  </verify>
  <done>Product detail page renders with swipeable image carousel, care instructions section, and related products. generateStaticParams enables ISR. Add to cart button present (functional after Plan 03).</done>
</task>

</tasks>

<verification>
`npm run build` must succeed with no errors.
`npm run dev` then:
1. `http://localhost:3000` — featured products + catalog grid render with seed data
2. `http://localhost:3000/shop` — sidebar + search + grid functional
3. `http://localhost:3000/shop/[any-seed-slug]` — product detail with carousel
4. `http://localhost:3000/pickup-info` — address, hours, instructions visible
Category filter via sidebar updates product list without full page reload.
</verification>

<success_criteria>
- Homepage: featured products + full catalog rendered from database
- Shop page: 2-3 column grid, left sidebar with category/instock/sort filters
- Product cards: image, name, price, category, stock badge, add-to-cart button
- Out-of-stock products: grayed out with badge, add-to-cart disabled
- Product detail: swipeable carousel, description, care instructions, related products
- Pickup info page: complete with address, hours, instructions
- Empty state: friendly message shown when no products match filter
- All pages mobile-responsive (tested at 375px width)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-mvp/01-02-SUMMARY.md` with what was built, key decisions, and exact file paths.
</output>
