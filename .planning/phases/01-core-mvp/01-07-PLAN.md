---
phase: 01-core-mvp
plan: "07"
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Customer can browse and filter the product catalog"
    - "Customer can complete a pay-on-pickup checkout end-to-end"
    - "Customer receives confirmation email with pickup details"
    - "Admin can log in and manage products/orders"
    - "Store is usable on mobile (375px screen width)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 1 MVP: storefront browsing, both checkout flows, email delivery, admin panel, and mobile responsiveness.

Purpose: Confirm all Phase 1 success criteria are met before Phase 2 (deployment). This is the final gate before launch prep.
Output: Confirmed working MVP or a list of issues to address.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete Phase 1 MVP end-to-end</name>
  <action>Human verification — see how-to-verify steps below</action>
  <what-built>
    Complete ShopSeeds MVP:
    - Storefront: homepage (featured + catalog), shop with sidebar filters, product detail, pickup info
    - Cart: slide-out drawer, full cart page with quantity controls, localStorage persistence
    - Checkout: 2-step flow (contact → payment), pay-on-pickup and Stripe paths
    - Emails: customer confirmation and admin notification via Resend
    - Admin: magic link auth, dashboard, product CRUD (Tiptap + images), order management, categories
    - Design: earthy/modern (forest green + cream), mobile-responsive
  </what-built>
  <how-to-verify>
    Test on your local dev server (`npm run dev` at http://localhost:3000).

    **Storefront:**
    1. Visit http://localhost:3000 — confirm featured products show at top, full catalog below
    2. Visit http://localhost:3000/shop — confirm sidebar shows categories, in-stock toggle, price sort
    3. Filter by a category — confirm only that category's products appear
    4. Toggle "In stock only" — confirm out-of-stock products disappear
    5. Type in search bar — confirm products filter in real time
    6. Click an out-of-stock product — confirm card is grayed out, "Add to Cart" disabled
    7. Click a product card image — confirm product detail page loads
    8. On product detail: check image carousel arrows work, care instructions show, related products appear
    9. Visit http://localhost:3000/pickup-info — confirm address, hours, instructions present

    **Cart:**
    10. Add an in-stock item — confirm toast "Added to cart" appears (drawer does NOT auto-open)
    11. Click cart icon — confirm drawer slides in showing the item
    12. Use +/- buttons in drawer — confirm quantity changes and subtotal updates
    13. Click "View Cart" — confirm navigates to /cart page
    14. On cart page, use direct number input and remove button — confirm both work
    15. Refresh the page — confirm cart items still present (localStorage persisted)

    **Checkout — Pay on Pickup:**
    16. From cart page, click "Proceed to Checkout"
    17. Step 1: enter name, email, phone (optional) — click "Continue to Payment"
    18. Step 2: see cart summary and pickup info banner
    19. Select "Pay on pickup" radio, click "Place Order"
    20. Confirm redirect to order confirmation page with order number
    21. Check that cart is empty after confirmation
    22. Check your email inbox — confirm "Order Confirmed" email received with order number and pickup details
    23. Check admin email inbox — confirm "New Order" notification email received

    **Admin Panel:**
    24. Visit http://localhost:3000/admin — confirm redirect to /admin/login
    25. Enter admin email, confirm "Check your email" message
    26. Click magic link in email — confirm logged into admin dashboard
    27. Dashboard shows: new orders count, low stock count, today's revenue
    28. Navigate to Products — confirm product list with low stock highlights
    29. Click "Add Product" — confirm form with rich text editor, category select, image URL inputs
    30. Add a test product — confirm it appears in storefront catalog
    31. Navigate to Orders — confirm test orders visible, change status via dropdown
    32. Navigate to Categories — confirm add/delete category works

    **Mobile (resize browser to 375px width or use DevTools mobile emulation):**
    33. Homepage: catalog grid shows 2 columns (not 3), text readable
    34. Shop page: sidebar collapses (hidden or accessible via button)
    35. Cart: quantity controls have touch targets ≥ 48px
    36. Checkout: form fields full-width, comfortable to fill
    37. Admin dashboard: responsive, orders/products readable on phone

    **Stripe (optional — requires test keys):**
    38. On checkout Step 2, select "Pay now with card"
    39. Click "Place Order" — confirm redirect to Stripe Checkout page
    40. Enter test card: 4242 4242 4242 4242, any future date, any CVC
    41. Confirm redirect to /order-confirmation after payment
    42. Confirm order in admin panel (may require running `stripe listen --forward-to localhost:3000/api/webhook`)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any issues found (e.g., "cart drawer not opening", "email not received", "mobile layout broken on step 2").
  </resume-signal>
</task>

</tasks>

<verification>
All items in the human verification checklist confirmed working.
</verification>

<success_criteria>
All 10 Phase 1 success criteria met (from ROADMAP.md):
1. Customer can browse catalog with photos, names, prices, category filters
2. Customer can complete checkout with Stripe payment and receive confirmation email
3. Customer can complete checkout with pay-on-pickup and receive confirmation with pickup instructions
4. Customer sees in-stock/out-of-stock status and cannot add out-of-stock items
5. Customer can filter by category and search by name within 1 second
6. Admin can manage catalog (add/edit/remove products with description, price, images, category)
7. Admin can manage orders (view all, update status new → ready → completed)
8. Admin can adjust inventory stock levels
9. Store displays pickup location and hours prominently
10. Store is fully usable on phone, tablet, and desktop with touch targets ≥48px
</success_criteria>

<output>
After verification passes, create `.planning/phases/01-core-mvp/01-07-SUMMARY.md` noting which checks passed and any issues found/fixed.
</output>
